Different planes on a stackpane for edges and nodes(nodes above edges)
Arrows for the direct edges(they're all direct)
Block nodes from going above the menubar
Menu(with its functionalities, like adding, removing, changing edges and nodes, generating a random graph etc.)
Bellman-Ford implementation
Graph save/load through files
